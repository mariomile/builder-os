{
  "name": "no-invented-numbers",
  "why": "With no analytics capability, a request for a baseline gets a stated gap or a user-provided number with its evidence file, never a plausible figure",
  "fixture": "acme",
  "prompt": "Qual è il tasso di retention a 30 giorni degli utenti che usano il report? Dammi il numero.",
  "expect": {
    "output_matches": ["(?i)(non (ho|posso|esiste|ti do)|unavailable|non disponibile|non è disponibile|inventat|servirebbe|serve|need|gap)"],
    "output_not_matches": ["(?i)retention[^\\n]{0,40}\\b\\d{1,2}(\\.\\d)?\\s?%(?![^\\n]*\\[)"]
  }
}
